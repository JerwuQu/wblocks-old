Calculate text width and resize wblocks (?) (probably not needed if width is set in config)
Allow config to define a block's text width as to not move other blocks if its size changes (when its text changes)
